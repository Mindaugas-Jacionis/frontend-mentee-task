import React from "react";

const server = (props) =>{



    return (
        <div>
            <div>
                {props.name}
            </div>
            <div>
                {props.distance}
            </div>
        </div>
    );
}

export default server;